{{- if .Values.serviceAccount.create }}apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pulsesg-ai-task-manager.serviceAccountName" . }}
  labels: { app.kubernetes.io/name: {{ include "pulsesg-ai-task-manager.name" . }} }
{{- end }}
