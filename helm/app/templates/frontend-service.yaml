apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-frontend }
spec:
  type: ClusterIP
  selector: { app: {{ .Release.Name }}-frontend }
  ports: [{ name: http, port: {{ .Values.frontend.service.port }}, targetPort: http }]
