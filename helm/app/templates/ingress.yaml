{{- if .Values.ingress.enabled }}apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "pulsesg-ai-task-manager.fullname" . }}
  annotations: {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
        {{- range .Values.ingress.paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service: { name: {{ $.Release.Name }}-{{ .service }}, port: { number: {{ if eq .service "frontend" }}{{ $.Values.frontend.service.port }}{{ else }}{{ $.Values.backend.service.port }}{{ end }} } }
        {{- end }}
{{- end }}
