apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-backend }
spec:
  type: ClusterIP
  selector: { app: {{ .Release.Name }}-backend }
  ports: [{ name: http, port: {{ .Values.backend.service.port }}, targetPort: http }]
